import { Converter } from "./Converter";

describe("here come all the integration tests for the converter feature", () => {
  it.todo("tests the “Update Value” use case");
  it.todo("tests the “Change Quote” use case");
  it.todo(
    "tests the “Refresh Rates” use case with real services and staging API"
  );
});
